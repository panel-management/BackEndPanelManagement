version: '3.8'

services:
  dev:
    container_name: club_panel_dev
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:3000"
    volumes:
      - ./src:/usr/src/app/src
    env_file:
      - .env
    environment:
      - NODE_ENV=development
