services:
  app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: dev:latest
    container_name: club_panel_dev
    ports:
      - "8000:3000"
    volumes:
      - ./src:/usr/src/app/src
    env_file:
      - .env
    environment:
      - NODE_ENV=development
